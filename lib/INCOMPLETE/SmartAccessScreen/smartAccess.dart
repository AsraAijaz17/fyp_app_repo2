import 'package:flutter/material.dart';

class SmartAccessScreen extends StatelessWidget {
  const SmartAccessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("SmartAccessScreen"),
      ),
    );
  }
}
