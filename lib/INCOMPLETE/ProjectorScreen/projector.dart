import 'package:flutter/material.dart';

class ProjectorScreen extends StatelessWidget {
  const ProjectorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ProjectorScreen"),
      ),
    );
  }
}
